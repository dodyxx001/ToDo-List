// Creates new TASK object

// import { getElements } from "./domelements";

const taskFactory = (text, date) => {

    return {
        // newTask,
        text: text,
        date: date,
    };
};



export {taskFactory};