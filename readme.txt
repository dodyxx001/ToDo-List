To - Do list app

live preview: https://dodyxx001.github.io/ToDo-List/

This is a to-do list app. It uses local storage to save tasks, 
so they do not disappear when we close the page.
The app uses object constructors, factory functions and
JS es6 modules. I also created a simple date-editor function
to render dates in more readable format.